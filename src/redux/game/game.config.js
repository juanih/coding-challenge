// MASTER SETTING (in MS)
export const INITIAL_MAX_ROBOT_RESPONSE_TIME = 800;       // (in ms): Maximum time will spend until Robot shots (initially)
export const INITIAL_MIN_ROBOT_RESPONSE_TIME = 600;       // (in ms): Maximum time will spend until Robot shots (initially)
export const INITIAL_MAX_PLAYER_RESPONSE_TIME = 1000;     // (in ms): Maximum time the player have to shot (initially)
export const DIFFICULT_RATE_PERCENTAGE = 10;              // Percentage in which the difficulty will be increased over each level
export const HITS_PER_LEVEL = 10;                         // How many hits will increase the difficulty level
export const REFRESH_MATCH_FREQUENCY = 10;                // (in ms): Refresh rate of the game status
